# Mail Admin Console

基于 **Spring Boot 4.0** + **Java 25** 构建的邮件服务器管理后台。

采用前后端不分离架构，使用 **Thymeleaf** 模板引擎 + **HTMX** 实现现代化无刷新交互体验。

## ✨ 功能特性

### 核心功能
- 📊 **仪表板** - 系统概览、实时统计、趋势图表
- 🌐 **域名管理** - 添加/编辑/删除邮件域名，启用/禁用控制
- 👥 **用户管理** - 用户 CRUD、密码重置、配额管理
- 📧 **邮件管理** - 邮件列表、详情查看、已读/删除标记
- 📤 **发送队列** - 队列状态监控、重试/取消发送
- 📝 **发送日志** - 投递记录查询、状态统计

### 技术亮点

#### 后端
- **Spring Boot 4.0** - 最新版本框架
- **Java 25** - 最新 LTS 版本
- **虚拟线程** - 高并发支持 (`spring.threads.virtual.enabled=true`)
- **Spring Security** - 完整的认证授权
- **Spring Data JPA** - 数据持久化
- **PostgreSQL** - 高性能数据库

#### 前端
- **Thymeleaf** - 服务端模板渲染
- **HTMX 2.0** - 无 JavaScript 实现 AJAX 交互
- **Thymeleaf Layout Dialect** - 页面布局复用
- **CSS 变量** - 主题化样式系统
- **响应式设计** - 适配各种屏幕尺寸

#### UI/UX
- 🌙 深色/浅色主题切换
- ⚡ 无刷新页面交互
- 🎨 赛博朋克风格设计
- 📱 响应式布局
- 🔄 实时数据自动刷新
- ✨ 流畅的动画效果

## 🚀 快速开始

### 前置要求
- Java 25+
- PostgreSQL 16+
- Maven 3.9+

### 启动步骤

1. **启动数据库**
   ```bash
   docker-compose up -d postgres
   ```

2. **编译运行**
   ```bash
   cd admin-web
   mvn spring-boot:run
   ```

3. **访问管理后台**
   - URL: http://localhost:8080
   - 默认账户: `admin` / `admin123`
   - 操作员账户: `operator` / `operator123`

## 📁 项目结构

```
admin-web/
├── src/main/java/com/yhm/adminweb/
│   ├── config/          # 配置类
│   │   ├── SecurityConfig.java
│   │   ├── WebConfig.java
│   │   └── GlobalExceptionHandler.java
│   ├── controller/      # 控制器
│   │   ├── DashboardController.java
│   │   ├── DomainController.java
│   │   ├── UserController.java
│   │   ├── EmailController.java
│   │   ├── QueueController.java
│   │   └── LogController.java
│   ├── dto/             # 数据传输对象
│   ├── entity/          # JPA 实体
│   ├── repository/      # 数据访问层
│   ├── service/         # 业务逻辑层
│   └── AdminWebApplication.java
├── src/main/resources/
│   ├── templates/       # Thymeleaf 模板
│   │   ├── layout/      # 布局模板
│   │   ├── dashboard/   # 仪表板
│   │   ├── domains/     # 域名管理
│   │   ├── users/       # 用户管理
│   │   ├── emails/      # 邮件管理
│   │   ├── queue/       # 发送队列
│   │   ├── logs/        # 发送日志
│   │   ├── common/      # 公共片段
│   │   ├── error/       # 错误页面
│   │   └── login.html   # 登录页
│   ├── static/          # 静态资源
│   │   ├── css/main.css
│   │   ├── js/main.js
│   │   └── images/
│   └── application.properties
└── pom.xml
```

## 🔧 配置说明

### 数据库配置
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/maildb
spring.datasource.username=postgres
spring.datasource.password=postgres
```

### 虚拟线程 (Java 21+)
```properties
spring.threads.virtual.enabled=true
```

### 安全配置
默认使用内存用户存储，生产环境建议：
1. 修改 `SecurityConfig.java` 使用数据库存储
2. 配置 HTTPS
3. 修改默认密码

## 📸 界面预览

### 仪表板
- 统计卡片：域名、用户、邮件、今日统计
- 存储使用环形图
- 发送队列状态
- 投递统计
- 最近邮件列表
- 存储排行榜

### 管理页面
- 数据表格支持搜索、筛选、分页
- 模态框表单编辑
- HTMX 无刷新操作
- Toast 通知反馈

## 🛠️ 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Spring Boot 4.0 |
| 语言 | Java 25 |
| 模板 | Thymeleaf 3.1 |
| 交互 | HTMX 2.0 |
| 安全 | Spring Security 6 |
| 数据 | Spring Data JPA |
| 数据库 | PostgreSQL 16 |
| 构建 | Maven 3.9 |

## 📄 License

MIT License

